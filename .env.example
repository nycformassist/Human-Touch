# SendGrid API key used by /api/submit-intake to email new intake submissions.
SENDGRID_API_KEY=

# Address that receives new intake submissions (defaults to valentine@thehumananchor.com).
INTAKE_TO_EMAIL=valentine@thehumananchor.com

# Verified SendGrid sender address the emails are sent FROM (required by SendGrid).
INTAKE_FROM_EMAIL=
